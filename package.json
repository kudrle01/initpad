{
  "name": "initpad",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "description": "InitPad – interní platforma pro vývojáře (IDP), prototyp k diplomové práci",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:api": "npm run start:dev --workspace @initpad/api",
    "dev:web": "npm run dev --workspace @initpad/web",
    "build": "npm run build --workspace @initpad/api && npm run build --workspace @initpad/web && npm run build --workspace @initpad/agent && npm run build --workspace @initpad/supervisor",
    "test": "npm run test:release-tools && npm run test --workspace @initpad/api -- --runInBand && npm run test --workspace @initpad/web && npm run test --workspace @initpad/agent && npm run test --workspace @initpad/supervisor",
    "test:release-tools": "node --test scripts/*.test.mjs",
    "lint": "eslint 'apps/{api,agent,supervisor}/src/**/*.ts' 'apps/web/src/**/*.{ts,tsx}' 'scripts/**/*.mjs' eslint.config.mjs",
    "format": "prettier --write 'apps/{api,agent,supervisor}/src/**/*.ts' 'apps/web/src/**/*.{ts,tsx}' 'scripts/**/*.mjs' eslint.config.mjs package.json 'apps/*/package.json'",
    "format:check": "prettier --check 'apps/{api,agent,supervisor}/src/**/*.ts' 'apps/web/src/**/*.{ts,tsx}' 'scripts/**/*.mjs' eslint.config.mjs package.json 'apps/*/package.json'",
    "audit:repo": "node scripts/repository-audit.mjs",
    "audit:public-release": "node scripts/audit-public-release.mjs",
    "check": "npm run audit:repo && npm run lint && npm run format:check && npm run build && npm run test",
    "check:release": "npm run check && npm audit --omit=dev --audit-level=low"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "overrides": {
    "@nestjs/platform-express": {
      "multer": "2.3.0"
    }
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.10.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "globals": "^17.12.0",
    "prettier": "^3.9.8",
    "typescript-eslint": "^8.70.0"
  }
}
