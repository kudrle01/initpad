import { projectName } from './project.js';

export default function App() {
  return (
    <main className="app">
      <h1>{projectName}</h1>
      <p>React app scaffolded by InitPad — built with Vite, served as static files.</p>
    </main>
  );
}
