# {{projectName}}

Vygenerováno platformou **InitPad** ze šablony *Node.js API (Express)*.

## Lokální spuštění
```bash
npm install
npm start
```
API běží na `http://localhost:8080` (`/` a `/health`).

## Nasazení
Řídí platforma přes promotion pipeline dev → test → prod
(provider Docker / SSH-VPS).
