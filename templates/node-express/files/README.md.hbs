# {{projectName}}

Generated by **InitPad** from the *Node.js API (Express)* template.

## Run locally
```bash
npm ci
npm start
```
The API runs at `http://localhost:8080` (`/` and `/health`).

## Deployment
Managed by the platform through the promotion pipeline dev → test → prod
(provider Docker / SSH-VPS).
