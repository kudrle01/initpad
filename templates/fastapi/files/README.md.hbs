# {{projectName}}

FastAPI (Python) REST API scaffolded by InitPad, served by uvicorn.

- Local: `pip install -r requirements.txt` then `uvicorn main:app --reload`
- Routes: `GET /` and `GET /health`

Runs on Docker across all environments.
