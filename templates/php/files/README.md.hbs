# {{projectName}}

PHP app scaffolded by InitPad, served by the built-in web server.

- Local: `php -S 0.0.0.0:8080 router.php`
- Routes: `GET /` and `GET /health`

Runs on Docker across all environments.
