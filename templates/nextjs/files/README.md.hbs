# {{projectName}}

Next.js (React) app scaffolded by InitPad.

- `npm ci` then `npm run dev` — local dev server
- Routes: `/` (page) and `/health` (route handler)

Runs on Docker across all environments.
