# {{projectName}}

NestJS (TypeScript) REST API scaffolded by InitPad.

- `npm ci` then `npm run start:dev` — local dev with watch
- `npm run build` — compile to `dist/`

Routes: `GET /` and `GET /health`. Runs on Docker across all environments.
