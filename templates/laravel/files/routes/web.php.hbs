<?php

use Illuminate\Support\Facades\Route;

Route::get('/', fn () => response()->json([
    'service' => '{{projectName}}',
    'status' => 'ok',
]));

Route::get('/health', fn () => response()->json([
    'status' => 'ok',
]));
