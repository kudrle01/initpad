# {{projectName}}

Nette (PHP) web project scaffolded by InitPad (via Composer at Docker build).

- The homepage (`/`) is Nette's default Homepage presenter.
- Runs on Docker across all environments.
