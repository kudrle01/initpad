# {{projectName}}

Django (Python) project scaffolded by InitPad, served by gunicorn.

- Local: `pip install -r requirements.txt` then `python manage.py runserver`
- Routes: `GET /` and `GET /health`
