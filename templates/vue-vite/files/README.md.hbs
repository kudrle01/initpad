# {{projectName}}

Vue (Vite) single-page app scaffolded by InitPad.

Dev/test run on Docker (nginx serving the build); prod is published as static
files over SFTP. `vite.config.js` uses `base: './'` so the app works under a subpath.
