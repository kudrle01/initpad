# {{projectName}}

Symfony (PHP) application scaffolded by InitPad (via Composer at Docker build).

- Routes: `GET /` and `GET /health` (JSON), from `src/Controller/AppController.php`.
- Runs on Docker across all environments.
