# {{projectName}}

Symfony golden path generated by InitPad with committed source and locked dependencies.

- `composer install`
- `vendor/bin/phpunit`
- `docker build .`

The container creates an ephemeral `APP_SECRET` when none is configured so a
fresh deployment can start without a shared secret baked into the image. Set a
persistent `APP_SECRET` through InitPad's environment secrets before storing
sessions or other signed data.
