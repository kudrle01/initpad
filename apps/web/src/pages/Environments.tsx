import { ComingSoon } from '@/components/ComingSoon';

export default function Environments() {
  return (
    <ComingSoon icon="server" title="Environments">
      A cross-project view of every dev, test and prod environment — what is
      running where, on which provider, with live status, version and URL. Today
      this lives on each project's detail page.
    </ComingSoon>
  );
}
